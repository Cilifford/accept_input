name: simple input 
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Your name'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello your name is ${{ github.event.inputs.name }}!'

    
      
